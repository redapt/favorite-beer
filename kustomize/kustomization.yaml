apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - redis
configmapGenerator:
- name: favorite-beer
  files: 
  - config/servicesettings.json
  literals:
    - REDIS_PORT=1234
    - REDIS_HOST=localhost
